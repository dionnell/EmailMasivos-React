# URL base del backend — incluye /api (prefijo global de NestJS)
VITE_API_URL=http://localhost:3000/api

# El dominio/correo remitente NO se configura acá — sale de MAIL_FROM
# en el .env del backend (Nest) y el frontend lo consulta vía GET /mail/status.
